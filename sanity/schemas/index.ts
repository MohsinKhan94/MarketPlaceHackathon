import product from "./product";
import page from "./page";
import cart from "./cart";
import checkout from "./checkout";
import navigation from "./navigation";

export const schemaTypes = [product, page, cart, checkout, navigation];
